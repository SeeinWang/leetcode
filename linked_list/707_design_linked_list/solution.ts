/*
 * 707. Design Linked List
 * https://leetcode.com/problems/design-linked-list/
 *
 * Design your implementation of the linked list. Support:
 * - get(index): Get value at index. Return -1 if invalid.
 * - addAtHead(val): Insert at head.
 * - addAtTail(val): Append at tail.
 * - addAtIndex(index, val): Insert before index-th node.
 * - deleteAtIndex(index): Delete the index-th node.
 *
 * Constraints:
 *     0 <= index, val <= 1000
 *     At most 2000 calls per method.
 */

class MyLinkedList {
    constructor() {

    }

    get(index: number): number {

    }

    addAtHead(val: number): void {

    }

    addAtTail(val: number): void {

    }

    addAtIndex(index: number, val: number): void {

    }

    deleteAtIndex(index: number): void {

    }
}
