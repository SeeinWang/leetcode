/*
 * 416. Partition Equal Subset Sum
 * https://leetcode.com/problems/partition-equal-subset-sum/
 *
 * Given an integer array nums, return true if it can be partitioned into two subsets
 * with equal sum.
 *
 * Example:
 *     Input: nums = [1,5,11,5]
 *     Output: true (subsets [1,5,5] and [11])
 *
 * Constraints:
 *     1 <= nums.length <= 200
 *     1 <= nums[i] <= 100
 */

function canPartition(nums: number[]): boolean {

};
