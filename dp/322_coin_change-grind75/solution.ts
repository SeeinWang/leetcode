/*
 * 322. Coin Change
 * https://leetcode.com/problems/coin-change/
 *
 * Given an array of coin denominations and a total amount, return the fewest number
 * of coins needed to make up that amount. Return -1 if it cannot be made up.
 *
 * Example:
 *     Input: coins = [1,5,11], amount = 11
 *     Output: 3 (11=5+5+1)
 *
 * Constraints:
 *     1 <= coins.length <= 12
 *     1 <= amount <= 10^4
 */

function coinChange(coins: number[], amount: number): number {

};
